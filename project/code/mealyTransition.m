function transitionParams = mealyTransition(currentState, oldState)
    transitionParams = currentState;
    %% Set ALL mealy transition parameters here, by state transition
    if(currentState == 1)
        % Reset all parameters to baseline here
        
    else
        % NEED TO POPULATE
        
    end
end %% end of function